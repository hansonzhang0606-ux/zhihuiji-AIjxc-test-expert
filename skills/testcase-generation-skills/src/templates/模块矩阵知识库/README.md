# 模块矩阵知识库

本目录为工程级长期**业务知识真源**（样例/本地默认 `kb_root`）。Stage 生成侧**禁止**全文通读；须经截取 Script 写入 `domain_facts.json`。

索引见上级 [`../README.md`](../README.md)。

| 文件 | 职责 |
|------|------|
| `模块矩阵总览.md` | 上：模块分层；下：支持矩阵；说明区默认不注入。不含匹配规则 |
| `{一级}/{二级}/页面关系.md` | 页面拓扑真源；Web URL 模板 |
| `主页面_*.md` / `子页面_*.md` | 核心元素（含后端接口列）+ 补充说明 |
| `知识库索引.json` | 本地可再生索引（不入远程 Git） |

需求→模块**如何匹配**见 `../模块匹配规则.md`，不进本库。

硬约定：端用 `web`/`app`；页面名须带「页」；不猜 URL/API；空字段 `-`；入库走 `skills/knowledge-base/` 两级审核。
